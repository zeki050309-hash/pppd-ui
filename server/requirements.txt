# PromptEar 서버 의존성
# 설치: pip install -r requirements.txt
#
# 주의: tensorflow 는 Python 3.9~3.11 에서 정상 동작합니다.
#       Python 3.12+ 는 tensorflow 설치 시 문제가 있을 수 있습니다.

# API 서버
fastapi==0.136.0
uvicorn[standard]==0.44.0

# 오디오 AI 모델
tensorflow==2.21.0
tensorflow-hub==0.16.1
# torch: transformers 호환을 위해 2.4.0 이상
torch>=2.4.0,<3
transformers==5.5.4

# 오디오 처리
numpy==1.26.4
scipy==1.13.1

# LLM / STT
anthropic==0.104.1
openai==2.38.0

# 기타
# pydantic: FastAPI(0.136)와 충돌하지 않도록 2.7 이상
pydantic>=2.7,<3
python-dotenv==0.21.0
# setuptools 81+ 는 일부 빌드에서 문제 → 81 미만 고정
setuptools<81
